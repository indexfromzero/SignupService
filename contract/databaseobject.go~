package contract

type IDatabaseObject interface {

	//Creates a new object
	create() error

	//Gets an existing object
	get(common.UUID) error

	//Updates an existing object
	update() error

	//Deletes an existing object
	delete() error
